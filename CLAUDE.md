# data-cleaner — 공공데이터 결측치 정리 실습기

> 한 줄 소개: **공공데이터포털에서 받은 자료의 결측치를 정리하고 필요한 속성만 골라 엑셀 파일로 만들어주는 데이터 정제 실습 웹앱**

## ① 앱의 목적과 대상 단원

- **목적**: 공공데이터포털에서 받은 원자료는 그대로 쓸 수 없다는 것을 학생이 직접 확인하고,
  ① 빈 칸(결측치)을 어떻게 다룰지 정하고 ② 탐구 질문에 필요한 속성(열)만 골라
  ③ 분석할 수 있는 깨끗한 엑셀 파일로 만드는 과정을 손으로 해 보게 한다.
- **대상**: 중학교 1~3학년 (교사 설명 없이도 [다음] 버튼만 눌러 끝까지 갈 수 있게 만들었다)
- **대상 단원**: 데이터 영역
- **연결 성취기준**
  - **[9정02-02]** 문제 해결에 적합한 데이터를 수집하고, 목적에 맞게 구분하여 관리한다.
    → 2단계(빈 칸 처리 규칙 정하기) + 3단계(필요한 열만 골라 새 파일로 저장)
  - **[9정02-04]** 사례를 중심으로 데이터 간의 관계를 파악하고, 데이터에 기반하여 의미를 해석한다.
    → **같은 자료인데 결측 처리 방법을 바꾸면 평균이 달라진다**는 것을 눈으로 보는 것이 이 앱의 핵심
- **이 폴더가 이 앱의 메인 저장 폴더다.** npm 설치물과 빌드 결과물도 모두 이 안에 둔다.
  프로젝트 루트(`C:\project_AI`)에는 아무것도 설치하지 않는다.
- **배포 주소**: **https://encosn.github.io/data-cleaner/** (저장소 `encosn/data-cleaner`)
  · 메인페이지 2단원에서 이 주소로 들어온다 → https://encosn.github.io/encosn/
  · 배포는 `.github/workflows/deploy.yml` 이 한다: `npm ci` → **`npm test`** → `npm run build` → `dist/`.
    **테스트가 깨지면 배포가 멈춘다.** `main` 에 push 하기 전에 `npm test` 를 돌려 볼 것.

### 이 앱이 만들어진 배경 (설계 판단의 근거)

사용자가 명시한 요구사항이 두 가지였고, 둘 다 설계의 중심에 있다.

1. **중학생이 다루기 쉬워야 한다** → 4단계 위저드, 큰 글씨·버튼, 되돌리기, 추천값 제시
2. **여러 셀이 병합되어 있더라도 셀 이름을 기준으로 값을 처리할 수 있게 해**
   → 이것이 이 앱의 기술적 존재 이유다. 아래 ②의 「병합 셀 해제」 항목 참고.

## 파일 구성

| 파일/폴더 | 설명 | 종류 |
|:---|:---|:---|
| `index.html` | 화면 골격 (4단계 섹션, 상·하단 고정 바, 확인 창, 토스트) | 소스 |
| `style.css` | CSS 변수 팔레트 + 컴포넌트. 교실 TV 기준 크기 | 소스 |
| `main.js` | 상태(`S`) · 단계 이동 · 렌더 · 클릭 처리 · 되돌리기. **계산은 하지 않는다** | 소스 |
| `lib/parse.js` | 엑셀 시트 → 열 이름 기준 표 (**병합 해제 · 머리글 찾기**). SheetJS·DOM 의존 0 | 소스 |
| `lib/clean.js` | 결측 판정 · 열 통계 · 추천 · 경고 · 「원본+설정=결과」 재계산 | 소스 |
| `lib/io.js` | 파일 읽기(인코딩·구분자·매직바이트) / xlsx·csv 저장. SheetJS를 쓰는 유일한 lib | 소스 |
| `vendor/xlsx-0.20.3.tgz` | **교실 방화벽 대비 백업.** `cdn.sheetjs.com` 이 막힌 PC에서 설치용 | 소스 |
| `tests/run-tests.mjs` | `lib/parse.js` 단위 테스트 62개. `npm test` 로 실행 | 소스 |
| `tests/break-tests.mjs` | 스스로 반례를 만들어 깨뜨려 보는 시험 | 소스 |
| `tests/make-files.mjs` | 시험용 엑셀 파일 생성기 (`tests/fixtures/` 에 만든다) | 소스 |
| `tests/perf.mjs` | 단계별 시간 측정 (`npm run test:perf`) | 소스 |
| `tests/fixtures/` | 위 생성기가 만든 시험용 파일. 지워도 `npm test` 가 다시 만든다 | 생성물 |
| `vite.config.js` | 개발 서버·빌드 설정 (문서 루트 `.`, **포트 8081**) | 소스 |
| `package.json` | 의존성과 npm 스크립트 | 소스 |
| `start-server.bat` | npm 없이 파이썬 서버로 `dist/` 열기 (보조) | 소스 |
| `CLAUDE.md` | 이 문서 | 소스 |
| `package-lock.json` | 설치 버전 고정 | 생성물 |
| `node_modules/` | 설치된 패키지 (xlsx + vite 등 17개) | 생성물 |
| `dist/` | `npm run build` 결과물 | 생성물 |

### 모듈 경계 규칙 (이걸 지켜야 테스트가 산다)

| 파일 | 하는 일 | 절대 하지 않는 일 |
|:---|:---|:---|
| `lib/parse.js` | 워크시트 객체 → `{columns, rows, …}`. 순수 함수, node에서도 돈다 | DOM 접근, SheetJS import, 입력 변형 |
| `lib/clean.js` | 결측/타입/추천/경고/`applyPlan`. 순수 함수 | DOM 접근, 파일 입출력 |
| `lib/io.js` | `File` → 워크북, 2차원 배열 → 내려받기 | 화면 문구 조립 (문구는 `notices` 배열로만 반환) |
| `main.js` | 상태 보관, 렌더, 클릭 처리, 문구 표시 | 통계 계산, 파싱 |

`lib/parse.js`·`lib/clean.js` 가 DOM에 손대지 않는 덕분에 node로 직접 테스트할 수 있다. 이 경계를 깨지 말 것.

## 실행 방법

**① npm 개발 서버 (기본)** — **이 폴더**에서
```
npm install     (처음 한 번)
npm start       → http://localhost:8081
npm run build   → 이 폴더의 dist/ 생성
npm run preview → dist/ 확인 (개발 서버를 먼저 끌 것)
```
루트에서 실행할 때는 `npm --prefix "./02.개인프로젝트/data-cleaner" start`.

**② 방화벽에 막힌 PC에서 설치**
```
npm install ./vendor/xlsx-0.20.3.tgz
npm install -D vite
```

**③ npm 없이 열기** — `npm run build` 를 먼저 한 뒤 `start-server.bat` 더블클릭 (파이썬 내장 서버, `dist/` 를 서비스).

> ⚠ **이 앱은 `index.html` 을 더블클릭해서 열 수 없다.** SheetJS 패키지를 import 하므로
> 반드시 개발 서버나 빌드 결과물(`dist/`)로 열어야 한다. 형제 앱 `binary-converter` 와 다른 점이다.
> 단, **인터넷은 필요 없다** — 외부 CDN 의존이 없어 번들에 전부 들어 있다. 교실 네트워크가 끊겨도 동작한다.

## ② 기능 목록

### 화면 구조 (4단계 위저드)

| 단계 | 패널 id | 내용 |
|:---|:---|:---|
| 1 파일 열기 | `panel1` | 드래그&드롭 / 연습용 예시 파일 / 시트 고르기 / 글자 방식 / **머리글 줄 확인** / 열 이름 목록 / 미리보기 |
| 2 빈 칸 정리 | `panel2` | 요약 카드 4장 / **빈 칸으로 볼 값 칩** / **합계 행 토글** / 열별 처리 방법 / 정리 전·후 미리보기 |
| 3 속성 고르기 | `panel3` | 탐구 질문 입력 / 속성 카드(코칭 문구) / 순서 바꾸기 / 열 이름 바꾸기 / 미리보기 |
| 4 엑셀 저장 | `panel4` | 정리 전·후 비교표 / **내가 한 일** 리포트 / 저장 설정 / xlsx·CSV 저장 / **확인 문제 4개** |

- 상단 진행 표시와 하단 내비게이션은 `position: sticky/fixed` 로 **항상 보인다**.
  1366×768 노트북에서 표를 스크롤해도 [다음] 버튼이 사라지지 않는다 —
  "다음이 어디 있어요?" 를 막는 가장 큰 장치다.
- 진행 표시 칩은 버튼이다. 완료한 단계는 눌러서 돌아갈 수 있고, 잠긴 단계는 흔들리며 이유를 알려 준다.
- `Alt+←/→` 단계 이동, `Ctrl+Z / Ctrl+Y` 되돌리기·다시 하기.

### 핵심 기능 ① 병합 셀 해제 (사용자 요구사항, 이 앱의 존재 이유)

공공데이터 엑셀은 보기 좋게 만들려고 셀을 많이 병합해 둔다. 병합된 셀은 **왼쪽 위 한 칸에만 값이 있고
나머지는 빈 칸**이라, 그냥 읽으면 "서울특별시"가 첫 줄에만 있고 나머지 줄은 비어 보인다.
`sheet_to_json` 도 채워 주지 않는다(실측 확인). 그래서 직접 푼다.

`lib/parse.js` 의 처리 순서:

| 순서 | 함수 | 하는 일 |
|:--|:---|:---|
| ① | `readGrid` | `!ref` + 실제 셀 주소 + 병합 범위를 **모두 합쳐** 표 크기를 정한다 (`!ref` 가 틀려도 안 죽는다) |
| ② | `unmergeCells` | 병합 범위의 좌상단 값을 범위 안 모든 칸에 복사. **원본을 바꾸지 않고 새 배열 반환** |
| ③ | `trimEmpty` | 완전히 빈 행·열 제거 (일부만 빈 칸은 결측치이므로 절대 안 지운다) |
| ④ | `detectTrailingNotes` | 표 아래 `※ 자료: …` 설명 줄 감지 |
| ⑤ | `detectHeaderRows` | 머리글이 몇 번째 행(들)인지 **점수로 추정 — 추천만 한다** |
| ⑥ | `buildColumnNames` | 여러 행 머리글을 합쳐 이름 만들기 (`인구` + `남` → `인구 남`), 중복 유일화, 빈 이름 자동 명명 |
| ⑦ | `parseSheet` | 위를 조합해 `{columns, rows, rowMeta, notices, headerInfo, excludedRows, stats}` 반환 |

**이후 모든 처리는 열 이름(문자열)을 열쇠로 한다. 셀 좌표를 쓰지 않는다.**

> ⚠️ **`Object.keys(row)` 로 열 순서를 얻지 말 것.** 자바스크립트는 `"2020"` 처럼 숫자로 보이는
> 열쇠를 객체 앞쪽으로 몰아넣어 순서가 뒤바뀐다. 열 순서가 필요하면 **항상 `columns` 배열**을 쓴다.
> (`toAOA`, `pickColumns` 가 그렇게 되어 있다)

### 핵심 기능 ② 「원본 + 설정 = 결과」 재계산 구조

**원본 `S.parsed` 는 절대 바꾸지 않는다.** 학생의 선택은 설정에만 쌓는다.

| 설정 | 내용 |
|:---|:---|
| `S.plan` | `{열이름: {action, value}}` — 열별 빈 칸 처리 방법 |
| `S.picked` | 3단계에서 고른 열 이름 (순서 그대로) |
| `S.tokens` | 빈 칸으로 보기로 한 표기 묶음 (`-`, `N/A` …) |
| `S.excludeTotals` | 합계 행을 뺄지 |
| `S.renames` | 바꿀 열 이름 |

화면을 그릴 때마다 `recompute()` 가 `applyPlan()` 으로 **원본에서 다시 계산**한다.
그래서 되돌리기가 공짜로 얻어지고, "누른 순서 때문에 결과가 달라지는" 버그가 원천적으로 없다.

- 처리 순서 고정: **① 채우기(모든 열) → ② 행 지우기(합집합)**
- 평균·중앙값은 **항상 원본 기준**으로 한 번만 계산한다 (다른 열의 처리에 영향받지 않는다)
- 결과가 두 가지다: `S.resultAll`(모든 열 — 2단계용) / `S.result`(고른 열 — 3·4단계용).
  섞으면 2단계에서 "열이 1개 줄었어요" 같은 엉뚱한 문구가 뜬다 (실제로 겪은 버그)

### 핵심 기능 ③ 한글 깨짐 방지

공공데이터포털 CSV는 **대부분 CP949(EUC-KR)** 다. 그냥 읽으면 `지역` 이 `������` 이 된다.

- `io.detectEncoding`: BOM 검사 → `TextDecoder('utf-8', {fatal:true})` 시도 → 실패하면 `euc-kr`
- 브라우저의 `euc-kr` 디코더 하나가 CP949·windows-949 를 모두 처리한다 (별도 라이브러리 불필요, 실측 확인)
- BOM(`\uFEFF`)을 떼지 않으면 첫 열 이름이 `﻿시도` 가 되어 **이름 대조가 전부 실패**한다
- 자동 판별이 틀렸을 때 학생이 `자동 / UTF-8 / CP949` 로 직접 바꿀 수 있다
- **CSV로 저장할 때는 맨 앞에 `\uFEFF` 를 붙인다.** 안 붙이면 집에서 엑셀로 열 때 다시 깨진다
- `XLSX.read` 에 **`dateNF: 'yyyy-mm-dd'` 를 반드시 준다.** 안 주면 날짜가 `7/29/26` 미국식으로 들어온다

### 핵심 기능 ④ 학습 피드백 (정답이 없는 도구형 앱에서)

| 겹 | 장치 | 어디에 |
|:--|:---|:---|
| 1 | **즉시 수치 피드백** — 조작할 때마다 행·열·빈 칸 숫자가 바로 변한다 | 2단계 요약 카드 |
| 2 | **정리 전/후 비교** — 같은 표를 토글로 전환, 지워질 행에 표시 | 2단계 미리보기, 4단계 비교표 |
| 3 | **코칭 메시지** — 규칙 기반 자동 판정 (`recommend`, `warnFor`, `coachColumn`) | 열 카드, 속성 카드 |
| 4 | **확인 문제 4개 + 「내가 한 일」 리포트** — 자기 자료에서 자동 생성, 보기 순서는 섞는다 | 4단계 |

**앱은 추천만 하고 결정은 학생이 한다.** 이 원칙이 코드에 박혀 있다:
- 위장 결측(`-`, `N/A`, `없음` …)은 **기본적으로 결측이 아니다.** 감지해서 개수를 보여 주고 학생이 칩으로 켠다
- **합계 행을 자동으로 지우지 않는다.** 찾아서 "평균이 40.8 → 25.5 로 달라집니다" 를 보여 주고 학생이 토글한다
  (연습용 예시 파일이 아니라 실제 통계표에서 이 장면이 수업의 하이라이트다)
- 경고는 막지 않는다. 알려 주고 학생이 그대로 진행할 수 있다

### 결측치 처리 방법 (열 성격에 따라 다르게 보여 준다)

| 열 종류 | 선택지 |
|:---|:---|
| 숫자 | 그대로 두기 / 빈 칸 있는 행 지우기 / **평균값** / **가운데 값(중앙값)** / 내가 정한 값 |
| 글자 | 그대로 두기 / 빈 칸 있는 행 지우기 / **「없음」으로 채우기** / 내가 정한 값 |
| 이름표 열(번호·코드·이름) | 그대로 두기 / 행 지우기 / 내가 정한 값 — **채우기를 막는다** (없는 대상을 만들게 되므로) |

- `0으로 채우기` 전용 버튼은 **일부러 만들지 않았다.** 버튼으로 두면 "제일 쉬워 보여서" 누른다.
  `내가 정한 값` 에 `0` 을 넣으면 "평균이 23.4 → 15.1 로 내려갑니다" 경고가 뜬다
- 최빈값 채우기는 v1 제외 (지역명 열에서 "가장 많은 값"이 조작되는 부작용이 교육적 이득보다 크다)
- 평균 채우기의 소수점은 **원본의 소수 자릿수를 따른다**(최대 2자리). 화면에 보인 값과 저장되는 값이 항상 같다

## ③ 현재 개발 상태와 다음 할 일

**상태: 1차 완성 (2026-07-29). 4단계 전 과정 동작하고 실제 파일로 검증까지 끝냈다.**

### 검증 내역 (실제로 돌려서 확인)

**`lib/parse.js` 단위 테스트 62개 통과 / 실패 0** — `npm test` 로 언제든 다시 돌릴 수 있다.
**코드를 고친 뒤에는 반드시 `npm test` 를 돌려라.** 아래 목록이 그대로 회귀 시험이 된다.
- 제목 병합 / 2행 머리글 / 3행 머리글 / 데이터 세로 병합 / 최악 조합 / 병합 없는 평범한 파일
- 열 이름 중복 유일화, 빈 이름 자동 명명, 줄바꿈·전각 공백 정리, 30열(AA~AD) 열 문자
- `!ref` 없음 / 틀림 / 병합이 범위 밖 / 병합 겹침 / 음수 좌표 / `!merges` 가 문자열
- `__proto__` 열 이름, 엑셀 날짜 일련번호 → `2024-01-31`, 원본 워크시트 불변, xlsx 왕복
- **20,000행 × 12열 + 병합 4,000곳 → 608ms**

**브라우저 실측 (http://localhost:8081, 콘솔 오류 0건)**

| 파일 | 결과 |
|:---|:---|
| 제목 가로병합 | 1행 제목 제외, 엑셀 2행을 머리글로, 3행 데이터 ✔ |
| 2행 병합 머리글 | `지역 / 인구 남 / 인구 여 / 면적` ✔ |
| 데이터 세로병합 | `서울특별시` 가 3개 행 모두에 채워짐 ✔ |
| 최악(제목+2단머리글+세로병합+빈행+주석+합계) | 6행 6열, 주석 2줄 제외, 합계 행 감지 ✔ |
| **CP949 CSV** | 인코딩 자동 판별 → **한글 정상 복원** ✔ |
| UTF-8 BOM CSV | BOM 제거, 첫 열 이름 깨끗함 ✔ |
| 열 이름 중복·빈 이름·줄바꿈 | `인구 / 인구(2) / 이름없음(C열) / 면적 (㎢) / 공백` ✔ |
| 3,000행 × 5열 (553KB) | 읽기 501ms, 2단계 렌더 114ms, 미리보기 20행만 ✔ |

- 저장물 검증: xlsx 는 `PK` zip 정상, CSV 는 앞 3바이트가 `EF BB BF`(BOM) 정상.
  CSV 내용 확인 — 병합 `시도` 전부 채움, 평균값 172·26480 채움, `-`·`N/A` 는 진짜 빈 칸으로 나감
- 되돌리기/다시 하기: 설정·통계·미리보기가 모두 정확히 되돌아옴
- 합계 행 토글: `학교 초등` 평균 **40.8 ↔ 25.5** 로 변하는 것 확인
- `npm run build` → 561 kB (gzip 184 kB)

### 개발 중에 실제로 고친 버그 (같은 실수 반복 방지 — 꼭 읽을 것)

| 버그 | 증상 | 원인과 교훈 |
|:---|:---|:---|
| **이름표 열 오판** | `자료수`·`좌석수` 같은 정상 숫자열이 "이름표 열"로 잡혀 **평균/중앙값 선택지가 아예 안 나왔다** | "값이 모두 다르면 이름표" 규칙을 숫자 열에 쓰면 안 된다. 교실 자료는 20~30행이라 숫자가 겹칠 일이 거의 없다. → 숫자 열은 **열 이름으로만** 판단 |
| **단계 간 상태 누출** | 2단계 요약에 `열(속성) 5 ▼ 1개 줄었어요` 가 떴다 (3단계 선택이 새어 들어옴) | 결과를 `S.result`(고른 열) / `S.resultAll`(모든 열) 로 분리 |
| **자기모순 추천** | 결측 75% 문자열 열에 `「없음」으로 채우기` 를 추천하고 곧바로 경고를 띄웠다 | 글자 열도 결측 50% 이상이면 `그대로 두기` 를 추천하게 바꿈 |
| **주석 판정이 데이터를 먹음** | `출처불명`·`비고사항`·`참고자료실` 같은 **정상 데이터 행이 조용히 사라졌다** | `출처|비고|참고|단위` 로 시작하면 주석으로 본 탓. → 낱말은 **뒤에 쌍점(:)이 있어야** 주석. `※ ＊ * 【 注` 기호는 쌍점 없이도 주석 |
| **보이지 않는 문자가 소스에 박힘** | 반복 헤더 감지가 동작하지 않았다. 한쪽 `join()` 안에 **U+0001 제어문자**가 들어가 서명이 영원히 달랐다 | 소스에 보이지 않는 문자를 직접 쓰지 말 것. `\uFEFF`·`\u00A0`·`\u3000`·`\u0001` 처럼 **명시적 이스케이프**로만 쓴다. 지금은 전 소스에 보이지 않는 문자가 0개다 (확인 스크립트로 검사했다) |
| **날짜 미국식 표기** | 날짜가 `7/29/26` 으로 들어왔다 | `XLSX.read` 에 `dateNF: 'yyyy-mm-dd'` 를 빼먹었다. `raw:false` 도 날짜를 망친다 → `cellValue()` 로 직접 처리 |
| **연도에 천 단위 쉼표** | `설립연도` 가 `1,998` 로 보였다 | `stat.isYear` 판정 추가 (열 이름이 연도류 + 값이 모두 네 자리 정수) |
| **퀴즈 정답이 항상 첫 보기** | 학생이 바로 눈치챈다 | `shuffled()` 로 보기를 섞는다. 문제는 한 번만 만들어 두므로 순서가 흔들리지 않는다 |

### 바로 다음에 할 일 (이어서 작업할 AI가 먼저 볼 것)

1. **저장한 `.xlsx` 를 실제 엑셀/한셀로 열어 확인** — 지금은 바이트 검증(PK zip)과 CSV 내용 확인까지만 했다.
   **우선순위 1위.** 열 너비, 한글, 숫자 서식, `정제기록` 두 번째 시트를 눈으로 볼 것.
2. **`lib/clean.js` 단위 테스트 추가** — `lib/parse.js` 는 62개가 있지만 `clean.js` 는 브라우저 실측만 했다.
   `isMissing` / `toNumber` / `analyzeColumn` / `applyPlan` / `detectTotalRows` 가 대상.
   `clean.js` 도 DOM 의존이 없어 node 에서 바로 import 된다.
3. 큰 파일에서 화면이 얼어붙는 문제 — `readWorkbook` 이 동기 작업이라 5만 행 파일에서 브라우저가 멈춘 듯 보인다.
   진행 표시(`파일을 읽고 있어요…`)를 실제로 띄우거나 Web Worker 로 옮기는 것을 검토.
4. 시트가 여러 개이고 첫 시트가 "일러두기"·"목차"인 파일 대응 (지금은 첫 시트를 자동 선택한다)

### v2 로 미룬 것 (v1에서 손대면 45분 수업이 무너진다)

- wide→long 변환 (연도가 열로 펼쳐진 표 녹이기) — 개념 난도가 중학생 1차시로는 과하다
- 지역명 표기 통일 사전 (`서울시`→`서울특별시`) — 사전 유지보수 부담 + 오치환 위험
- 이상치 탐지(IQR), 최빈값 채우기, 여러 파일 합치기, 데이터 시각화
- 날짜 형식 자동 통일 (지금은 경고 없이 원본 표기 유지)
- 중복 행 감지·제거 (판정 기준 설명에 시간이 걸린다)

### 현재 앱의 상태 (규칙이 아니라 사실)

- 외부 **CDN 의존 없음** → 인터넷이 끊긴 교실에서도 동작한다. 단 `index.html` 더블클릭은 안 된다(위 실행 방법 참고)
- 화면 문구 전부 한국어. 본문 21px / 제목 40px / 버튼 최소 높이 60px / 체크박스 32px (교실 TV·전자칠판 기준)
- 결측 셀은 **색 + 45° 사선무늬 + "빈 칸" 글자** 3중으로 표시 (색약 학생 고려)
- `localStorage`·`sessionStorage`·서버 전송 **전부 없음** → 새로고침하면 초기화. 학생 개인정보를 수집·저장하지 않는다
- 열 이름이 `이름·전화·주민·생년·주소·학번` 류면 "개인정보가 들어 있을 수 있는 열" 경고를 띄운다
  (규칙의 소극적 준수를 넘어 개인정보 보호를 능동적으로 가르치는 기능)
- SheetJS 는 **공식 CDN tarball 0.20.3** 을 쓴다. npm 레지스트리판은 0.18.5 에서 멈춰 있고 보안 권고가 있다.
  `npm audit` 취약점 0건
